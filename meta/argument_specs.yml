---
argument_specs:
  main:
    short_description: Install and configure UV from GitHub releases
    options:
      uv_version:
        type: str
        required: false
        default: "0.9.0"
        description: Version of UV to install
